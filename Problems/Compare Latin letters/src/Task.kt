import java.util.*

fun main(args: Array<String>) {
    val scanner = Scanner(System.`in`)
    // write your code here
    val first = scanner.next()[0]
    val second = scanner.next()[0]
    println(first.toLowerCase()==second.toLowerCase())
}